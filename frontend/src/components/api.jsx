// const prefix = process.env.REACT_APP_functionsURL;

// const post = (endpoint, body) =>
//   fetch(prefix + endpoint, {
//     method: "POST",
//     headers: { "Content-Type": "application/json" },
//     body: JSON.stringify(body),
//   }).then((res) => res.json());

// const get = (endpoint) => fetch(prefix + endpoint).then((res) => res.json());

// const api = { post, get };

// export default api;
